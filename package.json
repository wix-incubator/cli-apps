{
  "name": "cli-apps",
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "yarn wsrun --report build && yarn wsrun --report typecheck",
    "lint": "yarn wsrun --report -m lint",
    "lint:fix": "yarn wsrun --stages --report -m lint:fix",
    "test": "yarn wsrun run test",
    "clean": "rm -rf ./node_modules ./packages/*/node_modules"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "wsrun": "5.2.4"
  },
  "packageManager": "yarn@4.0.2"
}
